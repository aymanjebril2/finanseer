{
  "name": "server",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "bootstrap": "yarn install && cd server && yarn install && cd ../client && yarn install",
    "build:client": "cd client && yarn build",
    "start:server": "cd server && yarn start",
    "start:client": "cd client && yarn start",
    "start": "concurrently \"yarn start:client\" \"yarn start:server\"",
    "deploy:heroku": "cat client/heroku.gitignore > client/.gitignore && yarn build:client && git push heroku heroku:heroku && cat client/base.gitignore > client/.gitignore"
  },
  "devDependencies": {
    "concurrently": "^6.0.2"
  },
  "engines": {
    "node": "16.x"
  }
}
